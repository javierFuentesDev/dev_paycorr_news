import { NYTimesImageUrlPipe } from './nytimes-image-url.pipe';

describe('NYTimesImageUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new NYTimesImageUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
